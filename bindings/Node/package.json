{
  "name": "node-bindings",
  "version": "1.0.1",
  "description": "This folder is dedicated for libpointing Node.js bindings",
  "main": "builder.js",
  "devDependencies": {
    "jshint": "latest"
  },
  "scripts": {
    "install": "cd libpointing && npm install",
    "clean": "cd libpointing && node-gyp clean",
    "build": "cd libpointing && node-gyp rebuild",
    "lint": "jshint *.js server/*.js client/*.js"
  },
  "author": "Etienne Orieux",
  "license": "GPL-2.0",
  "private": true,
  "dependencies": {
    "ncp": "^2.0.0",
    "node-addon-api": "^2.0.0",
    "nw-builder": "^3.5.7"
  }
}
